# generated by the following command:
# php app/console generate:doctrine:entity \
#  --entity=AsyncTweetsBundle:User \
#  --fields="name:string(255) screen_name:string(255)" \
#  --with-repository --no-interaction --format=yml
AlexisLefebvre\Bundle\AsyncTweetsBundle\Entity\User:
    type: entity
    table: 'asynctweets_user'
    id:
        id:
            type: bigint
            id: true
    fields:
        name:
            type: string
            length: '255'
        screen_name:
            type: string
            length: '255'
        profile_image_url:
            type: string
            length: '255'
            nullable: true
        profile_image_url_https:
            type: string
            length: '255'
            nullable: true
    oneToMany:
        tweets:
            targetEntity: Tweet
            mappedBy: user
            cascade: [ 'all' ]
            orderBy:
                created_at: DESC
    lifecycleCallbacks: {  }
