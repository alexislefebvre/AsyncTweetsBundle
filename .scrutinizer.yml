checks:
  php: true

filter:
  excluded_paths:
    - Tests/*
  paths:
    - Command/*
    - Controller/*
    - Entity/*

build:
  tests:
    override:
      -
        command: 'phpunit --coverage-clover=coverage-clover'
        coverage:
          file: 'coverage-clover'
          format: 'php-clover'
